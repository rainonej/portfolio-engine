---
title: 'Sample writing post'
date: 2026-03-15
description: 'Single content fixture used to exercise the consumer-local /writing and /writing/[slug] replacement routes.'
tags: ['Demo', 'Routing']
---

This writing entry only exists so the replacement `/writing` and `/writing/[slug]` pages have real content to render.

The replacement pages live in `src/pages-local/writing/` and are declared in `src/registry/portfolio-engine.registry.json`.
