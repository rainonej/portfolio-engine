{
  "buildCommand": "pnpm --filter \"./packages/*\" run build && pnpm --filter demo-site build",
  "outputDirectory": "examples/demo-site/.vercel/output",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null
}
