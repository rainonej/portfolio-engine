## ADR-002: consumer site layers live under `src/`

## Decision

Consumer-owned semantic layers live under `src/`, while `public/` remains top-level for static public assets.

## Target

`src/config`, `src/content`, `src/context`, `src/registry`, `src/overrides`, `src/components`, `src/pages-local`, `public`, `.portfolio-engine`.
